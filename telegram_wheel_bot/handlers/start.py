from telegram import Update
from telegram.ext import ContextTypes
from telegram_wheel_bot.services.user_service import register_user


async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    u = update.effective_user
    register_user(u.id, u.username, u.first_name)
    text = (
        f"Привет, {u.first_name}! 👋\n"
        "Это бот для отслеживания качества твоей жизни.\n"
        "Выбери действие из меню ↓\n\n"
        "/start\n/about\n/Построить_колесо\n/Посмотреть_историю"
    )
    await update.message.reply_text(text)


async def about(update: Update, context: ContextTypes.DEFAULT_TYPE):
    text = (
        "📊 Wheel of Life Bot\n\n"
        """
        - Бот помогает регулярно оценивать 8 сфер жизни и видеть прогресс:
        Семья, Друзья, Здоровье, Хобби, Деньги, Отдых, Личное развитие, Работа/бизнес.
        - Каждую категорию оцениваешь от 1 до 10; бот строит диаграмму “колеса” и добавляет краткий AI-анализ.
        Основные команды:

        - /start — приветствие и краткое меню.
        - /about — описание бота.
        - /build_wheel или /Построить_колесо — построить новое колесо.
        - /history или /Посмотреть_историю — посмотреть историю.
        - /clean или /clear — удалить одно колесо или все.
        - /compaer — сравнить выбранное колесо с последним (см. ниже).
        Как построить колесо:

        - Выбираешь месяц из последних трех, для которых еще нет колеса (бот сам подсказывает).
        - Оцениваешь каждую из 8 категорий через кнопки 1–10.
        - После завершения бот:
        - сохраняет оценки,
        - строит диаграмму,
        - добавляет AI-анализ,
        - предлагает перейти к истории ( /history ).
        История и просмотр:

        - В /history бот показывает список твоих колес с датами.
        - При выборе колеса отправляет его название, изображение и анализ.
        - Запоминает “последнее открытое колесо” для команды сравнения.
        Сравнение месяцев:

        - Из истории доступна кнопка “Сравнить с последним”.
        - Команда /compaer :
        - сравнивает “последнее открытое колесо” с колесом предыдущего календарного месяца,
        - если предыдущее совпадает с выбранным, бот предупредит, что нельзя сравнить месяц сам с собой,
        - если “последнее открытое” не задано, бот подберет ближайшую пару из твоих колес.
        - В ответ приходит диаграмма с наложением двух месяцев.
        Удаление данных:

        - /clean или /clear — выбор конкретного колеса или “все колеса”.
        - Перед удалением бот спрашивает подтверждение; восстановить удаленные данные нельзя.
        AI-анализ:

        - Анализ строится на основе твоих оценок и формируется локальной LLM-моделью.
        - Текст анализа приводится в удобный формат для Telegram.
        Хранение данных:

        - Оценки и анализ сохраняются в локальной базе данных.
        - Изображения колеса хранятся в локальной папке.
        - В любой момент можно очистить данные через /clean .
        """         
    )
    await update.message.reply_text(text)
