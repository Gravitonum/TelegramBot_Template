# Telegram Бот "Wheel of Life" - Детальная Постановка для Cursor AI

## Обзор Проекта

**Цель:** Разработать Telegram бот на Python 3.12 для отслеживания качества жизни пользователя через "Колесо жизни" (Wheel of Life). Бот будет генерировать графики, сохранять историю оценок и предоставлять AI-анализ (через Ollama) для самооценки и планирования.

**Базовый репозиторий:** https://github.com/Gravitonum/TelegramBot_Template.git

**Стек технологий:**
- Python 3.12
- python-telegram-bot (latest)
- SQLite 3
- matplotlib / PIL для рисования графиков
- ollama (внешний API)
- dataclasses / Pydantic для моделей

---

## I. АРХИТЕКТУРА ПРИЛОЖЕНИЯ

### Структура проекта
```
telegram_wheel_bot/
├── config.py                  # Конфигурация (API токены, Ollama, DB)
├── database/
│   ├── __init__.py
│   ├── models.py             # SQLAlchemy / dataclass модели
│   └── repository.py         # CRUD операции
├── services/
│   ├── __init__.py
│   ├── user_service.py       # Регистрация, профиль пользователя
│   ├── wheel_service.py      # Логика построения колеса
│   ├── visualization.py      # Рисование колеса (matplotlib/PIL)
│   ├── llm_service.py        # Интеграция с Ollama
│   └── history_service.py    # Работа с историей
├── handlers/
│   ├── __init__.py
│   ├── start.py              # /start, /about
│   ├── wheel.py              # /Построить колесо
│   ├── history.py            # /Посмотреть историю, /сравнить с последним
│   └── admin.py              # /Clear (опционально)
├── prompts/
│   ├── __init__.py
│   ├── wheel_analysis.txt    # Промпт для анализа одного колеса
│   └── comparison.txt        # Промпт для сравнения двух колес
├── main.py                   # Точка входа, регистрация handlers
├── utils.py                  # Утилиты (валидация, форматирование)
└── requirements.txt          # Зависимости
```

---

## II. МОДЕЛИ ДАННЫХ

### Таблица `users`
```python
class User:
    id: int              # Primary Key, auto-increment
    telegram_id: int    # Unique, Telegram ID
    username: str       # Telegram username
    first_name: str     # Имя пользователя
    created_at: datetime
    updated_at: datetime
```

### Таблица `wheels`
```python
class Wheel:
    id: int                    # Primary Key
    user_id: int              # Foreign Key → users
    name: str                 # Название (например: "за декабрь")
    created_at: datetime
    llm_analysis: str         # Текст анализа от LLM
```

### Таблица `wheel_categories`
```python
class WheelCategory:
    id: int
    wheel_id: int            # Foreign Key → wheels
    category_name: str       # "Семья", "Друзья", и т.д.
    value: int              # Оценка от 1 до 10
    order: int              # Порядок в круге (0-7)
```

### Таблица `wheel_comparisons`
```python
class WheelComparison:
    id: int
    wheel_id_1: int          # Foreign Key → wheels (выбранное колесо)
    wheel_id_2: int          # Foreign Key → wheels (последнее колесо)
    comparison_analysis: str # Текст анализа сравнения
    created_at: datetime
```

---

## III. ДЕТАЛЬНОЕ ОПИСАНИЕ ФУНКЦИОНАЛА

### A. Регистрация пользователя

**Триггер:** `/start` команда

**Процесс:**
1. Получить `user_id` и `username` из объекта `message.from_user`
2. Проверить наличие пользователя в БД
3. Если нового — сохранить с `first_name`, `created_at`
4. Отправить приветственное сообщение с меню кнопок

**Кнопки главного меню:**
```
/start          - Главное меню
/about          - Информация о боте
/Построить_колесо - Начать новое колесо
/Посмотреть_историю - История колес
/Clear          - Очистить данные (admin only)
```

**Ответ:**
```
Привет, [first_name]! 👋
Это бот для отслеживания качества твоей жизни.
Выбери действие из меню ↓
```

---

### B. /about команда

**Процесс:**
1. Показать описание бота, его возможности и категории Wheel of Life

**Ответ:**
```
📊 Wheel of Life Bot

Этот бот помогает тебе оценить различные аспекты жизни и улучшить их.

Категории:
1. Семья
2. Друзья
3. Здоровье
4. Хобби
5. Деньги
6. Отдых
7. Личное развитие
8. Работа/бизнес

Оценивай каждую категорию от 1 до 10, и получай AI-анализ!
```

---

### C. /Построить_колесо команда

**Процесс:**

#### Шаг 1: Ввод названия колеса
1. Отправить сообщение: *"Назови свое колесо жизни (например: 'за декабрь 2024'). По умолчанию: предыдущий месяц год"*
2. Получить текст от пользователя (inline обработка)
3. По умолчанию: `f"{get_previous_month()} {current_year()}"`
4. Сохранить имя в состояние (используй `context.user_data`)

#### Шаг 2: Последовательный ввод оценок
1. Цикл по 8 категориям (в порядке часовой стрелки):
   - Семья → Друзья → Здоровье → Хобби → Деньги → Отдых → Личное развитие → Работа/бизнес

2. Для каждой категории:
   ```
   [Семья]
   Оцени по шкале от 1 до 10:
   ```
   
3. Создать inline кнопки с числами 1-10 (расположить в 2 ряда по 5)

4. При нажатии на число:
   - Сохранить значение в `context.user_data['wheel_scores']`
   - Перейти к следующей категории
   - Валидация: если не число или >10 — попросить повторить

5. После последней категории:
   - Сохранить в БД запись `wheels` и `wheel_categories`
   - Отправить: *"✓ Колесо создано! Рисую красивую диаграмму..."*

---

### D. Визуализация колеса (Wheel of Life Drawing)

**Требования:**
- Круг с 8 категориями, расставленными по часовой стрелке
- От центра к краю каждой категории проходит шкала 1-10
- Все 8 точек соединяются линиями (получается радар-граф / паутина)
- Закраска области внутри паутины полупрозрачным цветом (например, светло-голубой)
- Каждая линия имеет цвет, контрастный фону
- На краях категорий подписи: "Семья", "Друзья" и т.д. (по-русски)

**Инструмент:** matplotlib + PIL или plotly

**Кодовый пример (matplotlib):**
```python
def draw_wheel(scores: dict[str, int]) -> str:
    """
    scores: {"Семья": 8, "Друзья": 7, ...}
    Возвращает путь к сохраненному изображению
    """
    import matplotlib.pyplot as plt
    import numpy as np
    
    categories = list(scores.keys())
    values = list(scores.values())
    
    # Замкнуть цикл для графика
    values += values[:1]
    
    # Углы для каждой категории
    angles = np.linspace(0, 2 * np.pi, len(categories), endpoint=False).tolist()
    angles += angles[:1]
    
    # Нарисовать
    fig, ax = plt.subplots(figsize=(8, 8), subplot_kw=dict(projection='polar'))
    ax.plot(angles, values, 'o-', linewidth=2, color='#32a8d9')
    ax.fill(angles, values, alpha=0.25, color='#32a8d9')
    ax.set_xticks(angles[:-1])
    ax.set_xticklabels(categories)
    ax.set_ylim(0, 10)
    ax.set_yticks(range(1, 11))
    ax.grid(True)
    
    # Сохранить
    path = f"wheels/wheel_{wheel_id}.png"
    plt.savefig(path, dpi=150, bbox_inches='tight')
    plt.close()
    
    return path
```

**Сохранение:** `wheels/wheel_{wheel_id}_{timestamp}.png`

---

### E. LLM анализ колеса (Ollama Integration)

**Требования:**
1. Ollama хостится локально или удаленно (URL в `config.py`)
2. Использовать модель, указанную в конфиге (например, `mistral` или `llama2`)
3. Формировать промпт на русском языке
4. Получить ответ и сохранить в БД

**Промпт для анализа одного колеса (`prompts/wheel_analysis.txt`):**
```
Ты - профессиональный коуч личностного развития. 
Анализируя результаты колеса жизни, помогите пользователю понять:

1. Общее состояние: Какова общая балансировка жизни?
2. Сильные стороны: Какие аспекты жизни пользователя наиболее сбалансированы?
3. Область внимания: Какие 2-3 категории требуют наибольшего внимания?
4. Рекомендации: Какие конкретные действия пользователь может предпринять?

Результаты колеса жизни пользователя:
{wheel_scores_formatted}

Ответ дай в форме 3-4 параграфов, русский язык, тон мотивирующий и конструктивный.
```

**Формат входных данных:**
```
Семья: 8/10
Друзья: 7/10
Здоровье: 6/10
Хобби: 5/10
Деньги: 7/10
Отдых: 4/10
Личное развитие: 9/10
Работа/бизнес: 8/10
```

**Кодовый пример:**
```python
async def analyze_wheel_with_ollama(scores: dict[str, int]) -> str:
    """
    Отправить данные колеса в Ollama, получить анализ
    """
    import httpx
    
    # Форматировать результаты
    formatted_scores = "\n".join([f"{cat}: {val}/10" for cat, val in scores.items()])
    
    # Загрузить промпт
    with open("prompts/wheel_analysis.txt", "r", encoding="utf-8") as f:
        prompt_template = f.read()
    
    prompt = prompt_template.format(wheel_scores_formatted=formatted_scores)
    
    # Отправить в Ollama
    async with httpx.AsyncClient() as client:
        response = await client.post(
            f"{OLLAMA_URL}/api/generate",
            json={
                "model": OLLAMA_MODEL,
                "prompt": prompt,
                "stream": False,
            }
        )
    
    result = response.json()
    return result.get("response", "Ошибка анализа")
```

**Отправка результата пользователю:**
1. Отправить изображение колеса
2. Отправить текст анализа одним сообщением
3. Предложить кнопку "/Посмотреть_историю"

---

### F. /Посмотреть_историю команда

**Процесс:**
1. Получить все колеса пользователя из БД, упорядоченные по `created_at DESC`
2. Показать список в виде кнопок (встроенные кнопки или обычные)
3. Для каждого колеса: `{name} ({created_at})`

**Пример:**
```
Твоя история колес:
━━━━━━━━━━━━━━━━━━
🔹 за декабрь (2025-12-02)
🔹 за ноябрь (2025-11-02)
🔹 за октябрь (2025-10-01)
```

**При нажатии на колесо:**
1. Отправить изображение
2. Отправить текст анализа
3. Если это не последнее колесо (есть более поздний) — показать кнопку "/Сравнить_с_последним"

---

### G. /Сравнить_с_последним команда

**Условие:** Появляется только если выбранное колесо НЕ является самым новым

**Процесс:**
1. Получить последнее (самое новое) колесо пользователя
2. Нарисовать новое изображение с ДВЯ линиями:
   - **Выбранное колесо** (красный цвет, line width 2)
   - **Последнее колесо** (синий цвет, line width 2)
   - Легенда внизу графика

**Визуализация (пример кода):**
```python
def draw_wheel_comparison(scores_1: dict, scores_2: dict, name_1: str, name_2: str) -> str:
    """
    Нарисовать два колеса на одном графике
    scores_1 - выбранное колесо (красный)
    scores_2 - последнее колесо (синий)
    """
    import matplotlib.pyplot as plt
    import numpy as np
    
    categories = list(scores_1.keys())
    values_1 = list(scores_1.values())
    values_2 = list(scores_2.values())
    
    values_1 += values_1[:1]
    values_2 += values_2[:1]
    
    angles = np.linspace(0, 2 * np.pi, len(categories), endpoint=False).tolist()
    angles += angles[:1]
    
    fig, ax = plt.subplots(figsize=(10, 10), subplot_kw=dict(projection='polar'))
    
    # Выбранное колесо - красное
    ax.plot(angles, values_1, 'o-', linewidth=2, color='#e74c3c', label=name_1)
    ax.fill(angles, values_1, alpha=0.15, color='#e74c3c')
    
    # Последнее колесо - синее
    ax.plot(angles, values_2, 'o-', linewidth=2, color='#3498db', label=name_2)
    ax.fill(angles, values_2, alpha=0.15, color='#3498db')
    
    ax.set_xticks(angles[:-1])
    ax.set_xticklabels(categories)
    ax.set_ylim(0, 10)
    ax.set_yticks(range(1, 11))
    ax.grid(True)
    ax.legend(loc='upper right', bbox_to_anchor=(1.3, 1.1))
    
    path = f"wheels/comparison_{wheel_id_1}_{wheel_id_2}.png"
    plt.savefig(path, dpi=150, bbox_inches='tight')
    plt.close()
    
    return path
```

---

### H. LLM анализ сравнения (Comparison Analysis)

**Промпт для сравнения (`prompts/comparison.txt`):**
```
Ты - профессиональный коуч личностного развития.
Проанализируй изменения в колесе жизни пользователя за этот период.

Предыдущее колесо (от {date_1}):
{wheel_scores_1_formatted}

Текущее колесо (от {date_2}):
{wheel_scores_2_formatted}

Проанализируй:
1. Что улучшилось? Какие категории выросли?
2. Что ухудшилось? Какие категории снизились?
3. Тенденции: Видны ли закономерности в развитии?
4. Рекомендации: На что сосредоточить усилия в следующий период?

Ответ дай в форме 4-5 параграфов, русский язык, конструктивный тон.
```

**Кодовый пример:**
```python
async def compare_wheels_with_ollama(
    scores_1: dict, 
    scores_2: dict, 
    date_1: str, 
    date_2: str
) -> str:
    """
    Отправить два колеса в Ollama для сравнения
    """
    formatted_scores_1 = "\n".join([f"{cat}: {val}/10" for cat, val in scores_1.items()])
    formatted_scores_2 = "\n".join([f"{cat}: {val}/10" for cat, val in scores_2.items()])
    
    with open("prompts/comparison.txt", "r", encoding="utf-8") as f:
        prompt_template = f.read()
    
    prompt = prompt_template.format(
        date_1=date_1,
        date_2=date_2,
        wheel_scores_1_formatted=formatted_scores_1,
        wheel_scores_2_formatted=formatted_scores_2
    )
    
    # Вызов Ollama
    async with httpx.AsyncClient() as client:
        response = await client.post(
            f"{OLLAMA_URL}/api/generate",
            json={
                "model": OLLAMA_MODEL,
                "prompt": prompt,
                "stream": False,
                "options": {
                    "temperature": 0.7,
                    "top_p": 0.9,
                }
            }
        )
    
    result = response.json()
    return result.get("response", "Ошибка анализа")
```

**Отправка результата:**
1. Отправить графику сравнения (2 линии на одном графике)
2. Отправить текст анализа
3. Показать кнопку "/Посмотреть_историю" для возврата

---

### I. /Clear команда (опционально)

**Функция:** Очистить все данные пользователя (только если он admin)

**Процесс:**
1. Проверить, является ли `user_id` админом (список в `config.py`)
2. Если админ:
   - Удалить все записи пользователя из `wheel_categories`
   - Удалить все записи из `wheels`
   - Удалить саму запись из `users`
   - Отправить подтверждение: "✓ Все данные удалены"
3. Если не админ:
   - Отправить: "❌ У тебя нет прав на эту команду"

---

## IV. ПОТОК СОСТОЯНИЙ (Conversation States)

Используй `ConversationHandler` из `python-telegram-bot`:

```python
CHOOSING = 0
NAMING_WHEEL = 1
RATING_CATEGORY = 2
VIEWING_WHEEL = 3
COMPARING = 4
```

**Переходы:**
```
/start → CHOOSING
CHOOSING → /Построить_колесо → NAMING_WHEEL
NAMING_WHEEL → RATING_CATEGORY (цикл по 8 категориям)
RATING_CATEGORY → Колесо нарисовано, анализ выполнен, вернуться в CHOOSING

CHOOSING → /Посмотреть_историю → VIEWING_WHEEL (список)
VIEWING_WHEEL → Выбрать колесо → показать колесо, может быть кнопка /Сравнить_с_последним
COMPARING → анализ сравнения → вернуться в CHOOSING
```

---

## V. КОНФИГУРАЦИЯ (config.py)

```python
import os
from dotenv import load_dotenv

load_dotenv()

# Telegram
TELEGRAM_TOKEN = os.getenv("TELEGRAM_TOKEN")
ADMIN_IDS = [int(id) for id in os.getenv("ADMIN_IDS", "").split(",")]

# Database
DATABASE_URL = os.getenv("DATABASE_URL", "sqlite:///wheel_of_life.db")

# Ollama
OLLAMA_URL = os.getenv("OLLAMA_URL", "http://localhost:11434")
OLLAMA_MODEL = os.getenv("OLLAMA_MODEL", "mistral")

# Storage
WHEELS_DIR = os.getenv("WHEELS_DIR", "./wheels")
PROMPTS_DIR = os.getenv("PROMPTS_DIR", "./prompts")

# Logging
LOG_LEVEL = os.getenv("LOG_LEVEL", "INFO")

# Создать директории, если их нет
os.makedirs(WHEELS_DIR, exist_ok=True)
os.makedirs(PROMPTS_DIR, exist_ok=True)
```

**Файл `.env` (пример):**
```
TELEGRAM_TOKEN=your_bot_token_here
ADMIN_IDS=123456789,987654321
DATABASE_URL=sqlite:///wheel_of_life.db
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=mistral
WHEELS_DIR=./wheels
PROMPTS_DIR=./prompts
LOG_LEVEL=INFO
```

---

## VI. REQUIREMENTS.txt

```
python-telegram-bot==21.0
sqlalchemy==2.0.23
pydantic==2.5.0
python-dotenv==1.0.0
matplotlib==3.8.2
pillow==10.1.0
httpx==0.25.2
aiohttp==3.9.1
```

---

## VII. ОБРАБОТКА ОШИБОК И EDGE CASES

### 1. Ошибка подключения к Ollama
- При недоступности Ollama отправить пользователю: "⚠️ Сервис анализа недоступен. Попробуй позже."
- Сохранить колесо без анализа (поле `llm_analysis` = NULL)
- Позволить пользователю позже запросить анализ

### 2. Валидация ввода
- Если пользователь введет не число (1-10): "❌ Пожалуйста, введи число от 1 до 10"
- Если нажмет не на кнопку: "Используй кнопки для ввода"

### 3. Истечение timeout
- При долгом ожидании ответа от Ollama (>30 сек): показать спиннер, потом timeout-ошибка

### 4. История пустая
- Если у пользователя нет колес: "📭 История пуста. Создай свое первое колесо!"

### 5. Сравнение недоступно
- Если у пользователя только одно колесо: "📍 Недостаточно данных для сравнения. Создай еще одно колесо!"

---

## VIII. ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ (User Stories)

### Story 1: Первое использование
```
User: /start
Bot: Привет, Иван! 👋 Это бот для отслеживания качества твоей жизни...
[Показывает меню]

User: /Построить_колесо
Bot: Назови свое колесо (по умолчанию: ноябрь 2025):
User: за ноябрь

Bot: [Семья] Оцени по шкале от 1 до 10:
[Кнопки 1-10]
User: [нажимает 8]

Bot: [Друзья] Оцени по шкале от 1 до 10:
[Кнопки 1-10]
... (еще 6 категорий)

Bot: ✓ Колесо создано! Рисую диаграмму...
[Отправляет изображение колеса]
[Отправляет анализ от LLM]

User: /Посмотреть_историю
Bot: 🔹 за ноябрь (2025-12-02)
```

### Story 2: Сравнение колес
```
User: /Посмотреть_историю
Bot: 🔹 за декабрь (2025-12-02) [самое новое]
     🔹 за ноябрь (2025-11-02)

User: [нажимает на "за ноябрь"]
Bot: [Показывает старое колесо и анализ]
     [Кнопка: /Сравнить_с_последним]

User: /Сравнить_с_последним
Bot: [Рисует два графика на одном]
     [Отправляет анализ сравнения]
     "За месяц ты улучшил "Здоровье" с 6 до 8..."
```

---

## IX. ЗАМЕТКИ ДЛЯ РАЗРАБОТЧИКА (Вайбкодер Notes)

1. **Логирование:** Используй `logging` для всех операций (особенно LLM вызовы)
2. **Асинхронность:** Весь код на `async/await` (telegram-bot требует этого)
3. **Типизация:** Используй Type Hints везде (Python 3.12 позволяет это)
4. **Кэширование:** Кэшируй изображения колес (не перерисовывай каждый раз)
5. **Обработка конкурентности:** SQLite может быть медленным — можешь перейти на PostgreSQL позже
6. **Тестирование:** Напиши юнит-тесты для `visualization.py` и `llm_service.py`
7. **Документирование:** Добавь docstrings ко всем функциям

---

## X. ПРИМЕРЫ КОДА - SKELETON

### handlers/start.py
```python
from telegram import Update
from telegram.ext import ContextTypes, ConversationHandler

CHOOSING, NAMING_WHEEL, RATING_CATEGORY = 0, 1, 2

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Команда /start"""
    user = update.effective_user
    
    # Сохранить пользователя в БД
    user_service.register_user(
        telegram_id=user.id,
        username=user.username,
        first_name=user.first_name
    )
    
    # Отправить приветствие
    await update.message.reply_text(
        f"Привет, {user.first_name}! 👋\n"
        f"Это бот для отслеживания качества твоей жизни.\n"
        f"Выбери действие:",
        reply_markup=main_menu_keyboard()
    )
    
    return CHOOSING

def main_menu_keyboard():
    """Главное меню"""
    from telegram import ReplyKeyboardMarkup
    
    keyboard = [
        ["/start", "/about"],
        ["/Построить_колесо", "/Посмотреть_историю"],
        ["/Clear"]
    ]
    return ReplyKeyboardMarkup(keyboard, one_time_keyboard=False)
```

### services/wheel_service.py
```python
from datetime import datetime
from dateutil.relativedelta import relativedelta

class WheelService:
    CATEGORIES = [
        "Семья", "Друзья", "Здоровье", "Хобби",
        "Деньги", "Отдых", "Личное развитие", "Работа/бизнес"
    ]
    
    def get_default_wheel_name(self) -> str:
        """Получить имя колеса по умолчанию (предыдущий месяц)"""
        prev_month = datetime.now() - relativedelta(months=1)
        return f"за {self._month_name_ru(prev_month.month)} {prev_month.year}"
    
    def _month_name_ru(self, month: int) -> str:
        months = {
            1: "январь", 2: "февраль", 3: "март", 4: "апрель",
            5: "май", 6: "июнь", 7: "июль", 8: "август",
            9: "сентябрь", 10: "октябрь", 11: "ноябрь", 12: "декабрь"
        }
        return months[month]
    
    def create_wheel(self, user_id: int, name: str, scores: dict[str, int]) -> int:
        """Создать новое колесо и вернуть его ID"""
        # Логика сохранения в БД
        pass
    
    def get_user_wheels(self, user_id: int):
        """Получить все колеса пользователя"""
        pass
```

### services/visualization.py
```python
def draw_wheel(scores: dict[str, int], wheel_id: int) -> str:
    """Нарисовать колесо жизни"""
    # Реализация с matplotlib (см. раздел D выше)
    pass

def draw_wheel_comparison(
    scores_1: dict, 
    scores_2: dict,
    name_1: str,
    name_2: str,
    wheel_id_1: int,
    wheel_id_2: int
) -> str:
    """Нарисовать сравнение двух колес"""
    # Реализация (см. раздел G выше)
    pass
```

### services/llm_service.py
```python
import httpx
from config import OLLAMA_URL, OLLAMA_MODEL

class OllamaService:
    async def analyze_wheel(self, scores: dict[str, int]) -> str:
        """Анализ одного колеса"""
        # Реализация (см. раздел E выше)
        pass
    
    async def compare_wheels(
        self,
        scores_1: dict,
        scores_2: dict,
        date_1: str,
        date_2: str
    ) -> str:
        """Сравнение двух колес"""
        # Реализация (см. раздел H выше)
        pass
```

---

## XI. DEPLOYMENT

### Локальный запуск
```bash
pip install -r requirements.txt
python main.py
```

### Docker (опционально)
```dockerfile
FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["python", "main.py"]
```

### Требования к серверу
- Python 3.12+
- Ollama (на том же сервере или доступна по сети)
- SQLite или PostgreSQL
- ~500MB RAM для работы

---

## XII. FUTURE ENHANCEMENTS

1. **Экспорт в PDF/Excel** — позволить пользователю скачать отчет
2. **Еженедельные напоминания** — отправлять напоминание обновить колесо
3. **Социальная функция** — поделиться колесом с друзьями (аноним)
4. **Рекомендации** — интегрировать с внешними ресурсами (статьи, курсы)
5. **Уведомления** — когда категория падает ниже определённого уровня
6. **Экспоненциальный анализ** — выявлять тренды за 3-6 месяцев

---

**Конец постановки**

Готово к передаче в Cursor AI! 🚀
